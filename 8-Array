// Array
   
const myArr = [0, 1, 2, 3, 4, 5] 


console.log(myArr[4]);

// Array mrthods

//myArr.push(6) // use to adding
//myArr.pop() // use to remove last element 

myArr.unshift(10) // We used to add element first position mince before 0
myArr.shift() // We used to remove element first position mince before 0
console.log(myArr);
